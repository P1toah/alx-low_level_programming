#include "main.h"


*return: dest
*/
char *_strcat(char *dest, char *src)
{
char *ptr = dest;/*Function implementation*/
while (*ptr)
ptr++;
while (*src)
{
*ptr = *src;
ptr++;
src++;
}
*ptr = '\0';
return (dest);
}
